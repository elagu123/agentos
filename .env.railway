# Railway Environment Configuration
# Simple and cost-effective production setup

# Application
APP_NAME=AgentOS
ENVIRONMENT=production
DEBUG=false
PORT=8000

# Database (Railway provides PostgreSQL automatically)
# DATABASE_URL will be set automatically by Railway

# Redis (Railway addon - $3/month)
# REDIS_URL will be set automatically by Railway

# LLM APIs (same as before)
OPENAI_API_KEY=sk-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-key
DEFAULT_LLM_PROVIDER=openai
DEFAULT_MODEL=gpt-4o-mini

# Authentication (Clerk free tier)
CLERK_SECRET_KEY=sk_test_your-clerk-key
CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-key

# Security
JWT_SECRET=your-jwt-secret-here
ENCRYPTION_KEY=your-fernet-key-here

# Email (Resend free tier - 3k emails/month)
RESEND_API_KEY=re_your-resend-key
FROM_EMAIL=hello@yourdomain.com

# Monitoring (Sentry free tier)
SENTRY_DSN=https://your-sentry-dsn

# CORS
ALLOWED_ORIGINS=https://your-frontend-domain.railway.app,https://yourdomain.com